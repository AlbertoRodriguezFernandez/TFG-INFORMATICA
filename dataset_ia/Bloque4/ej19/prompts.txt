PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Escribe una clase en C++ llamada Punto2D que represente un punto en un plano cartesiano. Debe tener dos atributos privados x e y de tipo double. Implementa un constructor que inicialice ambos valores (con 0.0 por defecto si no se pasan argumentos) y sus respectivos getters. Además, implementa un método público double calcularDistancia(const Punto2D& otroPunto) que devuelva la distancia euclidiana entre el punto actual y el punto pasado como parámetro. Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Escribe una clase en C++ llamada Punto2D que represente un punto en un plano cartesiano. Debe tener dos atributos privados x e y de tipo double. Implementa un constructor que inicialice ambos valores (con 0.0 por defecto si no se pasan argumentos) y sus respectivos getters. Además, implementa un método público double calcularDistancia(const Punto2D& otroPunto) que devuelva la distancia euclidiana entre el punto actual y el punto pasado como parámetro. Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.


