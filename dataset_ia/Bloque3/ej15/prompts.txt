PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Escribe una función en C++ llamada void liberarMatriz(int**& matriz, int filas) que reciba por referencia un puntero doble que apunta a una matriz dinámica de enteros y la cantidad de filas. La función debe liberar correctamente la memoria de cada fila y luego la del array de punteros, evitando fugas de memoria. Finalmente, debe asignar nullptr al puntero original por seguridad. Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Escribe una función en C++ llamada void liberarMatriz(int**& matriz, int filas) que reciba por referencia un puntero doble que apunta a una matriz dinámica de enteros y la cantidad de filas. La función debe liberar correctamente la memoria de cada fila y luego la del array de punteros, evitando fugas de memoria. Finalmente, debe asignar nullptr al puntero original por seguridad. Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.


