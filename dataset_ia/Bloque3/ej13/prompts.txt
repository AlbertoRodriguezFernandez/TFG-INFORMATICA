PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Escribe una función en C++ llamada void invertirArray(int* arr, int size) que invierta el orden de los elementos de un array directamente en la misma memoria (in-place). Es obligatorio utilizar estrictamente aritmética de punteros para recorrer e intercambiar los valores; no se permite el uso de la notación de corchetes [] en el cuerpo de la función. Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Escribe una función en C++ llamada void invertirArray(int* arr, int size) que invierta el orden de los elementos de un array directamente en la misma memoria (in-place). Es obligatorio utilizar estrictamente aritmética de punteros para recorrer e intercambiar los valores; no se permite el uso de la notación de corchetes [] en el cuerpo de la función. Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.


