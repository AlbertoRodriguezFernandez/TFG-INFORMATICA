PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Define una estructura Nodo en C++ que contenga un entero dato y un puntero a Nodo llamado siguiente. Luego, escribe una función Nodo* crearNodo(int valor) que reserve memoria dinámicamente (heap) para un nuevo nodo, asigne el valor al campo dato, inicialice siguiente a nulo, y devuelva el puntero al nodo creado. Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Define una estructura Nodo en C++ que contenga un entero dato y un puntero a Nodo llamado siguiente. Luego, escribe una función Nodo* crearNodo(int valor) que reserve memoria dinámicamente (heap) para un nuevo nodo, asigne el valor al campo dato, inicialice siguiente a nulo, y devuelva el puntero al nodo creado. Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.


