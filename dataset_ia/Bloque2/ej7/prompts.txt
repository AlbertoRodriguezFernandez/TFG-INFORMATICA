PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Escribe una función void encontrarMaxMin(const int arr[], int size, int& maximo, int& minimo) que reciba un array de enteros estático de estilo C y su tamaño, y asigne a los parámetros por referencia el valor máximo y el valor mínimo encontrados en el array. Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Escribe una función void encontrarMaxMin(const int arr[], int size, int& maximo, int& minimo) que reciba un array de enteros estático de estilo C y su tamaño, y asigne a los parámetros por referencia el valor máximo y el valor mínimo encontrados en el array.
Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.

