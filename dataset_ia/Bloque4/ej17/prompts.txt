PROMPT BÁSICO:

Escribe una función en C++ que resuelva el siguiente problema: Escribe una clase en C++ llamada CuentaBancaria que gestione de forma segura un saldo. Debe tener un atributo privado saldo de tipo double. Implementa un constructor que inicialice el saldo (si el valor inicial es negativo, debe inicializarse a 0). Añade un método void depositar(double cantidad) que sume al saldo solo si la cantidad es positiva. Añade un método bool retirar(double cantidad) que reste la cantidad del saldo y devuelva true solo si hay fondos suficientes y la cantidad es positiva; en caso contrario, no modifica el saldo y devuelve false. Incluye un método double getSaldo(). Devuelve ÚNICAMENTE el código de la función, junto con los includes necesarios. No incluyas la función main() ni ningún texto o explicación adicional.

PROMPT DETALLADO:

Actúa como un desarrollador Senior experto en C++. Escribe una función óptima y segura en C++ moderno que resuelva el siguiente problema: Escribe una clase en C++ llamada CuentaBancaria que gestione de forma segura un saldo. Debe tener un atributo privado saldo de tipo double. Implementa un constructor que inicialice el saldo (si el valor inicial es negativo, debe inicializarse a 0). Añade un método void depositar(double cantidad) que sume al saldo solo si la cantidad es positiva. Añade un método bool retirar(double cantidad) que reste la cantidad del saldo y devuelva true solo si hay fondos suficientes y la cantidad es positiva; en caso contrario, no modifica el saldo y devuelve false. Incluye un método double getSaldo(). Debes cumplir estrictamente estos requisitos:
Asegúrate de que no haya fugas de memoria (memory leaks) ni accesos inválidos.
Aplica buenas prácticas de código limpio y control de errores.
Devuelve ÚNICAMENTE el código de la función y los includes necesarios.
No incluyas la función main() ni ningún texto explicativo.


